# Windsurf Rules for Sing-box Multi-Protocol Tools

完整开发指南请参考 `CLAUDE.md`。

## 项目特点

- 5 个独立的 Bash 管理脚本（VLESS、AnyTLS、Hysteria 2、Shadowsocks、EUserv HY2）
- 通过 `curl | bash` 远程执行，无构建系统
- 支持 9+ 种 Linux 发行版（Debian、Ubuntu、CentOS、Alpine 等）
- 无共享库设计：工具函数在脚本间复制

当前版本：v2.0.22 (2026-07-21)

## 不可移除的兼容性特性

每个脚本包含：
- Bash bootstrap (`exec bash "$0" "$@"`)
- CRLF 自动修复
- TTY 重定向 (`exec < /dev/tty`)
- busybox grep 兼容（不用 `-oP`）
- bash 3.x 兼容（不用 `${var,,}` 语法）

详见 `CLAUDE.md` 的"Every script has these quirks"章节。

## 开发流程

```bash
# 1. 本地测试（直接运行，不要用 install.sh）
bash vless.sh
bash hy2.sh

# 2. 静态验证
bash tests/validate_scripts.sh
git diff --check

# 3. VPS 集成测试（手动，在一次性实例上）
# 测试安装、升级、回滚、卸载、防火墙
```

## 版本同步检查清单

提交前确认已更新：
- [ ] 6 个脚本的文件头（版本 + 日期）
- [ ] 5 个脚本的菜单显示版本
- [ ] `euservhy2.sh` 的 `SCRIPT_VERSION`
- [ ] `tests/validate_scripts.sh` 的 `EXPECTED_VERSION`
- [ ] `README.md`（版本、日期、更新摘要）
- [ ] `CHANGELOG.md` 顶部条目

## Git 提交

- 主体用简体中文，类型前缀（`feat:`、`fix:`）用英文
- 示例：`fix: 修复 VLESS REALITY 目标选择逻辑`
- 禁止添加 `Co-Authored-By: AI ...`
- 不提交密码、IP、私钥、真实节点配置

参考文档：`CLAUDE.md`、`CONTRIBUTING.md`、`docs/ARCHITECTURE.md`、`docs/TESTING.md`
